name: CI — Build & Test

on:
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "tests/**"
      - "requirements.txt"
      - "pyproject.toml"
  push:
    branches:
      - "sdlc/**"
      - "wave-*/**"
    paths:
      - "src/**"
      - "tests/**"
      - "requirements.txt"
      - "pyproject.toml"

permissions:
  contents: read

jobs:
  build:
    name: Build Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Syntax check (py_compile)
        run: |
          echo "Running py_compile on all Python files in src/..."
          find src/ -name "*.py" -print0 | xargs -0 -I {} python -m py_compile {}
          echo "✅ All files pass syntax check"

      - name: Import validation
        env:
          GITHUB_MODELS_API_KEY: test-placeholder
          GITHUB_MODELS_ENDPOINT: https://models.inference.ai.azure.com
          SECRET_KEY: ci-test-secret-key-minimum-32-chars!!
        run: |
          echo "Validating top-level package imports..."
          python -c "
          import importlib, pathlib, sys
          failures = []
          for f in pathlib.Path('src').rglob('*.py'):
              if f.name.startswith('_'):
                  continue
              module = str(f.with_suffix('')).replace('/', '.')
              # Skip migration scripts (they need Alembic runtime context)
              if 'migrations.versions' in module or 'migrations.env' in module:
                  continue
              try:
                  importlib.import_module(module)
              except Exception as e:
                  failures.append((module, str(e)))
          if failures:
              for mod, err in failures:
                  print(f'❌ {mod}: {err}')
              sys.exit(1)
          else:
              print('✅ All modules importable')
          "

  test:
    name: Test Suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest pytest-asyncio httpx

      - name: Run tests
        env:
          GITHUB_MODELS_API_KEY: test-placeholder
          GITHUB_MODELS_ENDPOINT: https://models.inference.ai.azure.com
          SECRET_KEY: ci-test-secret-key-minimum-32-chars!!
        run: |
          if [ -d tests ] && find tests/ -name "test_*.py" | grep -q .; then
            python -m pytest tests/ -v --tb=short --strict-markers
          else
            echo "⚠️ No test files found — skipping (tests will be required once test suite exists)"
          fi

  lint:
    name: Lint (optional)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install linter
        run: pip install ruff

      - name: Run ruff
        run: |
          if [ -d src ]; then
            ruff check src/ --output-format=github
          fi
